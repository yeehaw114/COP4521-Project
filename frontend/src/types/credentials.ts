export type RegisterCreds = {
    firstName:string
    lastName:string
    email:string
    username:string
    password:string
}

export type LoginCreds = {
    username: string
    password:string
}